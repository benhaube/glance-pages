- name: Formula 1
  # Show a title header on mobile web browsers
  show-mobile-header: true
  # Optionally, if you only have a single page you can hide the desktop navigation for a cleaner look
  # hide-desktop-navigation: true
  columns:

    - size: small
      widgets:

        - $include: widgets/search.yml
        - $include: widgets/f1-last-race-results.yml
        - $include: widgets/rss-f1.yml

    - size: full
      widgets:

        - type: split-column
          widgets:
            - $include: widgets/f1-next-race.yml
            - type: group
              widgets:
                - $include: widgets/f1-driver-standings.yml
                - $include: widgets/f1-constructor-standings.yml
        - $include: widgets/reddit-f1.yml

    - size: small
      widgets:

        - $include: widgets/clock.yml
        - $include: widgets/weather.yml
        - $include: widgets/weather-aqi.yml
        - $include: widgets/weather-forecast.yml
        - $include: widgets/calendar.yml
